apiVersion: v1
kind: Secret
metadata:
  name: telegram-secrets
  namespace: trading
type: Opaque
data:
  # Base64 encoded values - replace with actual credentials
  TELEGRAM_BOT_TOKEN: "YOUR_BOT_TOKEN_BASE64"
  TELEGRAM_CHAT_ID: "YOUR_CHAT_ID_BASE64"
